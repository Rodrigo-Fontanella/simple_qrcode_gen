# This is a Simple QRCode Generator in Python
# Just fill the fields with URL, Matrix (default=20), External and Internal RGB colors to degrade PNG,
# Path to an optional image to generate a PNG with logo inside
# and an output Path.
